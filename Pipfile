[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
pandas = ">=2.0.0"
sqlalchemy = ">=2.0.0"
openpyxl = ">=3.1.0"
xlwings = ">=0.30.0"
pyodbc = ">=5.0.0"
xlsxwriter = ">=3.1.0"
python-dotenv = ">=1.0.0"

[dev-packages]
pytest = ">=7.4.0"
pytest-cov = ">=4.1.0"
black = ">=23.0.0"
flake8 = ">=6.0.0"
mypy = ">=1.5.0"

[requires]
python_version = "3.9"
